The model has the same number of unknowns and equations: 2048
The model could not be deduced to be symbolically well-posed.
The model has
  2020+4*(size(world.x_label.lines, 1)+size(world.y_label.lines, 1)+size(  world.z_label.lines, 1))
scalar unknowns and
  2005+(if sps1.fixedRotationAtFrame_a then 12 else 3)+(if sps1.fixedRotationAtFrame_b   then 12 else 3)+(if s1.lineForce.fixedRotationAtFrame_a then 12 else 3)+(if   s1.lineForce.fixedRotationAtFrame_b then 12 else 3)+(if Connections.isRoot(  pointMass.frame_a.R) then 12 else 3)+4*size(world.x_label.lines, 1)+4*size(  world.y_label.lines, 1)+4*size(world.z_label.lines, 1)
scalar equations.
However, exploiting the given numerical settings of parameters gives the same number of unknowns and equations:
  2048
Check of <a href="Modelica://ModelicaTest.MultiBody.Forces.SpringDamperSeries">ModelicaTest.MultiBody.Forces.SpringDamperSeries</a> successful.

