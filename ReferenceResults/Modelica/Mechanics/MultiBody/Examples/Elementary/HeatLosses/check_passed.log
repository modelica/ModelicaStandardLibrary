The model has the same number of unknowns and equations: 3001
The model could not be deduced to be symbolically well-posed.
The model has
  2973+4*(size(world.x_label.lines, 1)+size(world.y_label.lines, 1)+size(  world.z_label.lines, 1))
scalar unknowns and
  2877+(if  not Connections.isRoot(body1.frame_a.R) then 13 else 22)+(if   spring1.lineForce.fixedRotationAtFrame_a then 12 else 3)+(if spring1.lineForce.fixedRotationAtFrame_b   then 12 else 3)+(if damper1.fixedRotationAtFrame_a then 12 else 3)+(if   damper1.fixedRotationAtFrame_b then 12 else 3)+(if springDamper.fixedRotationAtFrame_a   then 12 else 3)+(if springDamper.fixedRotationAtFrame_b then 12 else 3)+(if    not Connections.isRoot(body2.frame_a.R) then 13 else 22)+(if springDamperSeries.fixedRotationAtFrame_a   then 12 else 3)+(if springDamperSeries.fixedRotationAtFrame_b then 12 else 3)  +(if  not Connections.isRoot(body3.frame_a.R) then 13 else 22)+(if   spring.lineForce.fixedRotationAtFrame_a then 12 else 3)+(if spring.lineForce.fixedRotationAtFrame_b   then 12 else 3)+4*size(world.x_label.lines, 1)+4*size(world.y_label.lines, 1)  +4*size(world.z_label.lines, 1)
scalar equations.
However, exploiting the given numerical settings of parameters gives the same number of unknowns and equations:
  3001
Check of <a href="Modelica://Modelica.Mechanics.MultiBody.Examples.Elementary.HeatLosses">Modelica.Mechanics.MultiBody.Examples.Elementary.HeatLosses</a> successful.

