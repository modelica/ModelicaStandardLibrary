The model has the same number of unknowns and equations: 1393
The model could not be deduced to be symbolically well-posed.
The model has
  1365+4*(size(world.x_label.lines, 1)+size(world.y_label.lines, 1)+size(  world.z_label.lines, 1))
scalar unknowns and
  1337+(if spring.lineForce.fixedRotationAtFrame_a then 12 else 3)+(if   spring.lineForce.fixedRotationAtFrame_b then 12 else 3)+(if  not   Connections.isRoot(body.frame_a.R) then 13 else 22)+4*size(world.x_label.lines,   1)+4*size(world.y_label.lines, 1)+4*size(world.z_label.lines, 1)
scalar equations.
However, exploiting the given numerical settings of parameters gives the same number of unknowns and equations:
  1393
Check of <a href="Modelica://Modelica.Mechanics.MultiBody.Examples.Elementary.SpringWithMass">Modelica.Mechanics.MultiBody.Examples.Elementary.SpringWithMass</a> successful.

